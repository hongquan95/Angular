import { TopDirective } from './top.directive';

describe('TopDirective', () => {
  it('should create an instance', () => {
    const directive = new TopDirective();
    expect(directive).toBeTruthy();
  });
});
